# BalanceFoldersToDisks
Smart PowerShell script to balance folders across fixed-size archive disks with growth buffer support.

# ⚖️ BalanceFoldersToDisks

PowerShell-скрипт **`BalanceFoldersToDisks.ps1`** — умное распределение папок по архивным дискам с учётом будущего роста и жёстких ограничений объёма.  
Скрипт балансирует загрузку томов, предотвращает переполнение и даёт читаемый план архивации.

---

## 🎯 Назначение

Автоматизирует подготовку данных к записи на фиксированное число носителей (внешние HDD, Blu-ray, сетевые тома и т.п.), решая задачу:

> **Как равномерно распределить папки разного размера по N дискам ёмкостью X ГБ, чтобы оставить запас места на рост ключевых каталогов?**

---

## ✨ Ключевые особенности

- **Точный расчёт реальных размеров** всех файлов в папках (с обходом подкаталогов).
- **Буфер роста**: для указанных папок автоматически резервируется дополнительное место (например, +100 ГБ).
- **Интеллектуальное распределение**:
  - Основано на **модифицированном Best-Fit Decreasing** — папки размещаются на томе с наибольшим свободным местом, куда они ещё помещаются.
  - Включает **пост-оптимизацию**: мелкие папки перемещаются для уменьшения разброса между дисками.
- **Наглядный вывод**:
  - Отображается **прогнозируемый** и **текущий** объём на каждом диске,
  - Папки с буфером помечаются как `[+буфер]`,
  - В конце — итоговый **разброс в гигабайтах**.
- **Безопасность**: проверка существования путей, обработка недоступных каталогов.

---

## ⚙️ Настройка

Все параметры задаются в начале файла `BalanceFoldersToDisks.ps1`:

```powershell
# Список папок для архивации
$folders = @("J:\Сериалы", "I:\Book", "J:\Фото", ...)

# Папки, которые будут пополняться — к ним добавится буфер
$growingFolders = @("J:\Сериалы", "I:\Book")

# Размер буфера на растущую папку (в гигабайтах)
$bufferSizeGB = 100

# Количество целевых дисков
$diskCount = 5

# Максимальный объём одного диска (в гигабайтах)
$diskSize = 4000
```

Просто отредактируйте значения под вашу задачу.

---

## 📋 Пример вывода

```text
ДИСК 2: 2378/4000 GB (прогноз)
    (сейчас: 2278 GB)
  1598.6 GB - J:\Азиатские [+буфер]
  525.1 GB - I:\Course
  154.3 GB - J:\Фото

...

БАЛАНСИРОВКА (с буфером): разброс 43.5 GB
```

- **Прогноз** — объём с учётом буфера роста (используется для планирования).
- **Сейчас** — текущий реальный объём на диске.
- Разброс **< 50 ГБ** при общем объёме ~12 ТБ — отличный баланс.

---

## ▶️ Как использовать

1. Скачайте [`BalanceFoldersToDisks.ps1`](BalanceFoldersToDisks.ps1).
2. Откройте его в редакторе и настройте параметры (пути, буфер, число и размер дисков).
3. Запустите в PowerShell:
   ```powershell
   .\BalanceFoldersToDisks.ps1
   ```
> 💡 При первом запуске может потребоваться разрешить выполнение скриптов:  
> `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`

---

## 📜 Лицензия

Скрипт распространяется по лицензии **No** — свободно используйте, изменяйте и интегрируйте в свои проекты.

---

> 💾 **Совет**: полученный план можно использовать для ручной копии или автоматизировать архивацию через `robocopy`, `7-Zip` или другие инструменты.

--- 

Теперь название скрипта и документация полностью согласованы. Готово к публикации на GitHub!
